% amount of training digits per class
training_size = 10;
% amount of test digits per class
%test_size = 5;
% image size
d_size = 50;
% total size of objects per class
x = [1:1000];

prwaitbar off;


    disp(fprintf( 'Total %d, correct: %d, err: %d\n, accuracy: %d\n', test_size*10, true_hog, false_hog, e));

%% p value for comparison of the classifiers
%run the command[p,h]=signrank(accuracy_SVM,accuracy_KNN) where SVM and KNN
%accuracy are stored in the respective matrices obtained from "true"
%matrices