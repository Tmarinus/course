function [ error ] = nist_eval( filename,w,n )
% filename is received from the processed dataset
% w is the given classifier 
% n is the desired number of digits per class to be tested
% error is the classification error for this combination of inputs

%% Preprocessing
% preprocess training and test sets and flatten the cell arrays

% create corresponding label cell arrays
%[training, training_labels] = my_rep(prnist(0:9,1:n), d_size);
% randomly chosing training and testing data
[training, training_labels] = my_rep(prnist(0:9,randperm(1000,n)), d_size); 

% Test from leftover main testset
% [test, test_labels] = my_rep(prnist(0:9,n+1:1000), d_size);
% randomly chosing training and testing data
[test, test_labels] = my_rep(prnist(0:9,setdiff(x,randperm(1000,n))), d_size);
end

